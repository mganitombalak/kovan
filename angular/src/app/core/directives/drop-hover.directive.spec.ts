import { DropHoverDirective } from './drop-hover.directive';

describe('DropHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new DropHoverDirective();
    expect(directive).toBeTruthy();
  });
});
