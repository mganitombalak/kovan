import { ModalBodyContainerDirective } from './modal-body-container.directive';

describe('ModalBodyContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalBodyContainerDirective();
    expect(directive).toBeTruthy();
  });
});
