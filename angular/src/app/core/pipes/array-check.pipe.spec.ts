import { ArrayCheckPipe } from './array-check.pipe';

describe('ArrayCheckPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayCheckPipe();
    expect(pipe).toBeTruthy();
  });
});
